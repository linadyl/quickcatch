{
  "name": "quickcatch",
  "version": "1.0.0",
  "description": "QuickCatch NHL highlights app",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && NODE_ENV=production USE_DUMMY_DATA=true node server.js",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "postbuild": "node build.js",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "author": "",
  "license": "ISC"
}